#include <iostream>
#include <string>
#include <cmath>
#include <iomanip>
using namespace std;

int main (){
	int y, z;

    cout << "Enter a number: ";
    cin >> y;
    z = y;

    cout << "Multiplication table of " << y << ":\n";

    for (int x = 1; x <= 10; ++x)
    {
        cout << z << " * " << x << " = " << (y * x) << "\n";
    }
	
}
